# BCT 30Hz 실험 기록

2026-08-14~ / 서버·속도 세팅은 [A100_TRAINING_NOTES.md](../../A100_TRAINING_NOTES.md) 참고.

## 데이터셋

| 이름 | 내용 | 상태 |
|---|---|---|
| `..._joint_30hz_clean` (+`_train`/`_val`) | **현행 기준 데이터셋.** 아래 30hz에서 정지 구간 제거 + 재균형. 1,315클립 / 522,521프레임, 태스크별 104.6k(편차 0.05%) | **사용 중** |
| `..._joint_30hz_aug_clean` (+`_train`/`_val`) | 위와 동일 클립에 46차원 state (기존 31 + base_gravity 3 + FK EE 12). raw root/quat/ee는 parquet 별도 컬럼으로 보존. train/val은 clean 스플릿을 액션 해시로 1:1 복제 | **라운드2 사용 중** |
| `datasets/RooibosT/G1_Dex1_BCT_subtask_joint_30hz` (+`_train`/`_val`) | 30fps 재추출본. 1,319클립 / 541,791프레임, v2.1. 샤드 머지 file_index 버그 수리 완료(매핑 복원 + 전체 재인코딩, 전수 검증). **정지 구간 미제거** | 구버전(비교용) |
| `datasets/RooibosT/G1_Dex1_BCT_subtask_joint` | 15Hz 원저자본, v2.1 변환 + 손상 4개 수리 | 보관 |
| `datasets/RooibosT/G1_Dex1_BCT_subtask_ee` | 15Hz EE 14차원, v2.1 변환 완료 (스캔/수리 미실시) | 학습 보류 |
| `datasets/G1_WBT_Dex1_Building-Children-Table` | 원본 parquet/meta만 (3.3GB, 비디오 삭제) — cmd_vel 유도 등 재가공용 | 보관 |

## 학습된 / 학습 중 모델

공통 레시피: effective batch 256, lr 1e-4, warmup 0.05, wd 1e-5, cosine, state_dropout 0.2,
horizon 40, 3뷰(cam_head + 양 손목), state 31차원, 4×A100 DDP (`--use-ddp`), seek approximate.

### 1. joint_30hz — 완료 ✅
- **전 키 ABSOLUTE** (waist+arms+grippers 19차원 @30Hz)
- 50k 스텝 상한 중 13,477에서 조기 종료. **최적 = ckpt-7500** (4.1 epochs)
- val open-loop: 액션 MSE 0.0295, 팔 MAE 4.4~4.7°, 허리 0.66°, 그리퍼 0.21u(0-4.5), h1 MAE 0.037rad
- 호라이즌 오차 성장 h1→h40: 0.037→0.120 → **배포 재추론 주기 10~20스텝 권장**
- HF: `RooibosT/gr00t-n1.7-g1-dex1-bct-joint-30hz-h40` (private)
- **교훈: eval_loss(flow-matching 목적함수)는 7.5k 이후 올라갔지만 액션 지표는 정체(악화 아님).
  체크포인트 선택은 eval_loss가 아니라 open-loop 스캔(`eval_val_mse.py`)으로 할 것**

### 2. joint_30hz_relarm — 완료 ✅ **(open-loop A/B 승리)**
- **팔 RELATIVE + 허리·그리퍼 ABSOLUTE** (`g1_dex1_bct_joint_relarm_config.py`)
- 근거: CK-Sung brainco 모델(팔 REL + 핸드 ABS)의 실배포 성공. redblock의 ABS>>REL ablation은
  배포 교란이 있었음(README도 A40을 "confounded"로 기록) → 교란 없는 재검증
- 12,502스텝에서 종료(eval_loss 반등 기준), **최적 = ckpt-12500** (스캔상 마지막까지 개선 중이었음)

| 지표 (val 66eps, open-loop) | relarm ckpt-12500 | joint_30hz ckpt-7500 (ABS) | 차이 |
|---|---|---|---|
| 액션 MSE | **0.0270** | 0.0295 | −8% |
| MAE | **0.0753** | 0.0826 | −9% |
| **first-5 MAE** | **0.0282** | 0.0422 | **−33%** |
| 팔 MAE | **0.0700 (4.0°)** | 0.0790 (4.5°) | −11% |
| 그리퍼 MAE | 0.208 | 0.215 | 동급 |

- **RELATIVE가 근미래(청크 초반) 정확도에서 특히 강함** — 예측이 현재 상태에 앵커되므로 구조적 이점.
  최종 판정은 실기 closed-loop 비교로.

### 3. joint_30hz_relarm_4view — 폐기(교란) ❌
- relarm(승자 구성) + **cam_head_right 추가로 4뷰**, 30,000스텝(16.5 epochs), bf16 통신 적용 3.4s/it
- 스텝 2,500 스캔: MSE 0.03458 / f5 0.0349 / 팔 0.0821 (3뷰 동일 스텝: 0.03536 / 0.0370 / 0.0881) — 초기 우세
- **주의(교란)**: max_steps를 50k→30k로 줄여 cosine 스케줄이 압축됨 (warmup 2,500→1,500스텝,
  같은 스텝에서 lr이 다름). 따라서 중간 체크포인트끼리의 비교에는 스케줄 효과가 섞여 있고,
  이 런의 성과는 "4뷰 + 30k 어닐링 스케줄"의 합으로 봐야 함. 뷰 단독 효과를 보려면
  3뷰를 동일한 30k 스케줄로 재학습해야 함.
- 다만 목적이 "어느 정도 스텝이 적정한가"의 탐색이므로 이 교란은 감수 가능. 판정은 30k 어닐링 종점 수치로.

### 4. 라운드1 뷰 개수 ablation — 완료 ✅ **(3뷰 채택)**
3view(GPU 4-6, 64/GPU) vs 4view(GPU 0-3, 48/GPU), **양쪽 effective batch 192 / 15k / cosine 완주**로
스케줄 교란 제거. 각 12h22m / 12h26m. 동일 val 66eps 스캔.

| ckpt | MSE 3v→4v | first-5 MAE | 팔 MAE | 그리퍼 MAE |
|---|---|---|---|---|
| 2500 | −4.08% | −3.59% | −1.91% | −5.56% |
| 5000 | +0.01% | +1.53% | +0.07% | −1.51% |
| 7500 | −2.96% | −1.34% | +0.06% | −3.68% |
| 10000 | −3.02% | +0.43% | −1.58% | −1.00% |
| 12500 | −2.81% | +0.18% | −0.07% | −1.54% |
| 15000 | −1.35% | +0.46% | **+0.00%** | −0.77% |

15k 절대값: MSE 0.03005→0.02964, 팔 MAE 0.06952→0.06953, 그리퍼 0.2015→0.1999.
- **팔은 완전 동률**(다섯째 자리까지), first-5는 오히려 4뷰가 근소 열세 — 배포 시 실행하는 건 청크 앞부분
- 일관되게 이긴 건 그리퍼뿐이나 절대 개선 0.0016u = 0~4.5 스케일의 **0.036%**
- 비용은 GPU-시간 +33% + 카메라 1대. → **3뷰 채택.** 실기에서 깊이 판단 실패가 보이면 재검토
- 양쪽 모두 15k에서 **아직 단조 개선 중**(3뷰 12.5k→15k MSE −1.7%) → 최종 학습 30k 근거

### 5. 라운드2 aug 46차원 — 진행 중 🔄
`joint_30hz_relarm_3view_aug`, GPU 0-3(48/GPU, eff. 192, 15k), 2.71 s/it.
대조군은 **라운드1의 3view arm을 재사용**(max_steps/lr/warmup/wd/scheduler/accum/seed 전부 동일,
`per_device 64×ws3` vs `48×ws4`로 effective batch만 같고 샘플러 분할 순서가 다름 — seed 수준 차이).
`aug_clean_train/val`은 새로 나누지 않고 **clean 스플릿을 액션 해시로 1:1 복제**(1249/66, 비디오 하드링크)
→ 같은 에피소드·같은 val 윈도우. 판정 기준: 1~2% 수준 차이는 배치 순서 요동과 구분 불가이므로
**최종 구간에서 일관되게 벌어질 때만** 채택.

### ⚠️ 가장 중요한 교훈: eval_loss로 판단하지 말 것
세 런에서 eval_loss(flow-matching 목적함수)와 실제 액션 품질이 **서로 다른 방향으로** 갈렸다:
- joint_30hz: eval_loss 7.5k 이후 상승, 액션 지표는 **정체**(악화 아님)
- relarm: eval_loss 7.5k 이후 상승, 액션 지표는 **12.5k까지 계속 개선**
- 라운드1 3뷰/4뷰: 양쪽 다 eval_loss 반등, 액션 지표는 **15k까지 단조 개선**

**왜 갈리는가** (`gr00t_n1d7.py:228-278`):
```python
noise = randn(...); t = (1 - Beta(1.5,1.0)) * 0.999
loss = mse(model((1-t)*noise + t*actions, t, obs),  actions - noise)
```
1. **답이 원리적으로 없는 구간이 손실을 지배한다.** 타깃 `actions - noise`의 두 항을 모델은
   둘 다 못 본다(섞인 `x_t` 하나만 봄). t가 작으면 최적 예측조차 큰 잔차를 남기고, 이
   환원 불가능한 항이 손실 크기를 지배해 개선 가능한 부분을 덮는다.
2. **추론은 4스텝 Euler 적분이라 자기 교정적이다**(`:397`). 매 스텝 실제 도달한 `x_t`를
   다시 읽으므로 앞 스텝 오차가 회수된다. eval_loss는 각 t를 독립 채점해 이를 반영하지 않는다.
3. **eval_loss 상승 = 멀티모달 평균에서 특정 모드로 뾰족해진 것.** val 샘플이 다른 모드면
   회귀 오차는 커지지만, 그 모드가 옳으면 정답 궤적 하나와 비교하는 open-loop MSE는 좋아진다.
   (MSE가 같이 나빠지지 않았다는 사실 자체가 "옳은 쪽으로 뾰족해졌다"는 증거)
+ eval_loss는 매 평가마다 `noise`/`t`를 새로 뽑는 몬테카를로 추정치라 자체 분산도 있다.
  open-loop 스캔은 윈도우별 diffusion seed 고정이라 훨씬 조용하다.

→ 체크포인트 선택도, **조기 종료 판단도** `eval_val_mse.py` open-loop 스캔으로 할 것 (체크포인트당 ~3분).
→ VLA는 액션 재현 정확도가 목표라 목적함수가 평평해져도 더 긴 스케줄이 이득일 수 있다
   (OpenVLA도 같은 이유로 스텝을 크게 늘림). **다음 학습은 스텝 상한을 넉넉히 두고
   2,500스텝마다 스캔으로 판단**하는 방식을 기본으로.

### 참고 기준점
- 15Hz joint (원저자): `RooibosT/gr00t-n1.7-g1-dex1-bct-joint-h40`
- CK-Sung brainco: 17k프레임/43epochs/effective 64, REL arms, 실배포 양호

## 데이터 품질 이슈와 조치 (2026-08-16)

### A. 텔레옵 정지 구간 — 제거함
팔 액션이 비트단위로 얼어붙은 구간이 학습 가중치를 왜곡. 30Hz 실측: 0.5초 이상 정지가
91 runs / 7,667프레임, 최장 **62.2초**(단일 정지가 horizon-40 윈도우 1,827개 = 전체의 0.39%).
윈도우 기준 오염도 0.92% (466,575개 중 4,307개가 1.33초간 팔 0.5° 미만).
- 임계값 판정이 깨끗함: 0.5°→2°로 4배 올려도 해당 윈도우가 4,307→4,340개 (이봉분포)
- **RELATIVE에서 더 해로움**: 타깃이 정확히 0 → "출력 0" 지름길. ABSOLUTE는 현재 자세라 정보가 남음
- 조치: 정지 지점에서 **클립 분할**(이어붙이지 않음) → 새 불연속 없음을 실측 확인
  (프레임당 최대 팔 점프 33.01°로 원본과 동일, >5° 비율 1.844%→1.866%)
- exporter도 근본 수정: url_lerobot `--min-stall` (commit a4d5199)

### B. aug state 46차원 — 재설계
초기 50차원(root 7 + ee 12)에 세 가지 결함이 있어 아래로 교체:
| 제거/교체 | 실측 근거 |
|---|---|
| root x,y 제거 | 클립간/클립내 std 비율 **90.6× / 55.9×** → 사실상 클립 ID (leakage) |
| root z 제거 | 비율 1.1배지만 다리 관절의 결정론적 함수, 전체 변동 36mm |
| root_quat → **base_gravity** | quat은 yaw 지배(yaw std 109° vs roll 0.63°/pitch 1.07°), w·z 성분이 거의 순수 yaw. projected gravity는 yaw 불변 |
| ee_state → **FK 재계산** | 기록된 EE가 팔 관절보다 **10프레임(333ms) 지연**. URDF FK 대조: lag+10에서 3.0mm 일치, lag0에서 22.3mm 불일치 → 같은 행 안에서 모순. FK 적용 후 lag0 잔차 **0.0000mm** |
- FK 규약: G1 `g1_body29_hand14.urdf`, **waist=0 고정(몸통 좌표계)**, wrist_yaw 원점 + 로컬 x 0.05m, extrinsic xyz euler
- **base_gravity만이 진짜 새 정보** (관절각으로 복원 불가한 몸통 기울기). EE는 정보량 0의 재파라미터화

## 앞으로 실험 후보 (우선순위순)

**확정된 것 (재실험 불요)**: relarm(팔 RELATIVE) 채택, 30Hz, horizon 40, 정지 필터 적용.
h16/h32와 cmd_vel은 기각(아래).

**라운드 방식**: ablation은 15k 코사인으로 양쪽 완전 어닐링 후 비교(공정 + 시간 절반),
최종 배포 모델만 30k. 판정은 항상 open-loop 스캔.

0. ~~라운드1 — 뷰 개수~~: **완료, 3뷰 채택**(위 4번). 이전 3v4 비교는 lr 스케줄 교란
   (warmup 2500 vs 1500, 12.5k에서 lr 8.95e-5 vs 6.75e-5)이 있어 폐기 — 교란은 항상 짧은
   스케줄에 유리하므로 "4뷰가 지면 결론, 이기면 미결"의 한쪽 추론만 가능했음
1. **[진행 중] 라운드2 — aug state**: 3뷰 승자 vs 승자+46차원(`..._3view_aug`). base_gravity 효과 확인
2. **라운드3 — 최종 30k**: 라운드2 승자 구성으로 effective batch 256. 15k에서 아직 개선 중이었음
3. **ee (14차원 EE)**: 데이터 준비됨. 보류 사유 — 배포 시 IK 필요, joint 대비 이점 불명확, 레포 기본도 joint
4. **state_dropout 0.1/0.3**: 실기에서 시각/상태 의존 편차가 보일 때만
5. ~~h16/h32~~: 30Hz에서 h16=0.53s로 너무 짧음. 반응성은 h40 청크의 앞 10~20스텝만 실행하는 배포 전략으로 해결
6. ~~cmd_vel (odometry 미분)~~: **실측 기각** — 30Hz 유한차분 SNR 0.2 + 추정기 점프 글리치. 차기 데이터 녹화 때 오퍼레이터 명령을 직접 채널로 기록해서 해결

## 차기 데이터 녹화 시 추가할 채널 (원본 데이터셋의 한계 보완)

- **cmd_vel (vx, vy, ωz)**: 오퍼레이터 베이스 이동 명령 — 로코모션 학습의 전제
- **관절 속도 dq / 토크**: G1 LowState에 이미 있음, 기록 비용 낮음 (진단·후속 알고리즘용)
- 첫 프레임 센서 워밍업 (쿼터니언 [0,0,0,0] 아티팩트 방지), 프레임 단위 서브태스크 라벨링
